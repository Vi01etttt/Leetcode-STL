# 复盘记录

## 滑动窗口四问

- 窗口是什么：`s[left...right]`。
- 窗口维护什么：窗口内没有重复字符。
- 什么时候收缩：准备加入的字符已经存在于窗口中。
- 什么时候更新答案：消除重复并加入当前字符后。

## 长度公式

左右端点都包含在窗口中，因此长度是：

```cpp
right - left + 1
```

例如 `left = 2`、`right = 4`，窗口包含下标 `2、3、4`，长度为 `3`。

## 关键性质

`left` 和 `right` 都只向右移动，不会后退。这是滑动窗口能够做到 `O(n)` 的关键。
